# ASP VBScript 运行时测试用例

## 测试环境

- **项目**: asp-lite (Rust 实现的 Classic ASP 运行时)
- **服务器地址**: http://127.0.0.1:8080
- **测试时间**: 2026-03-05

---

## 一、基础输出测试

### 1.1 简单文本输出
**文件**: `test_001_simple_output.asp`

```asp
<%
Response.Write "Hello, World!"
%>
```

**预期结果**: 页面显示 `Hello, World!`

---

### 1.2 HTML 混合输出
**文件**: `test_002_html_mix.asp`

```asp
<html>
<body>
    <h1>ASP 测试页面</h1>
    <p>当前时间: <%= Now %></p>
    <%
        Response.Write "<p>这是动态生成的内容</p>"
    %>
</body>
</html>
```

**预期结果**: 正确渲染 HTML 结构，显示动态内容

---

## 二、变量声明测试

### 2.1 Dim 声明
**文件**: `test_003_dim.asp`

```asp
<%
Dim name, age
name = "张三"
age = 25
Response.Write "姓名: " & name & "<br>"
Response.Write "年龄: " & age
%>
```

**预期结果**: 正确显示变量值

---

### 2.2 未定义变量（Empty 值）
**文件**: `test_004_undefined.asp`

```asp
<%
' 未定义变量在算术运算中应被当作 0
result = 10 + undefinedVar
Response.Write "10 + undefined = " & result  ' 应显示 10
%>
```

**预期结果**: 未定义变量在算术运算中作为 0 处理

---

## 三、控制流测试

### 3.1 If/Then/Else
**文件**: `test_005_if.asp`

```asp
<%
Dim score
score = 85

If score >= 90 Then
    Response.Write "优秀"
ElseIf score >= 80 Then
    Response.Write "良好"
ElseIf score >= 60 Then
    Response.Write "及格"
Else
    Response.Write "不及格"
End If
%>
```

**预期结果**: 显示 "良好"

---

### 3.2 Select Case
**文件**: `test_006_select.asp`

```asp
<%
Dim dayOfWeek
dayOfWeek = 3  ' 假设 3 = 星期三

Select Case dayOfWeek
    Case 1
        Response.Write "星期一"
    Case 2
        Response.Write "星期二"
    Case 3
        Response.Write "星期三"
    Case Else
        Response.Write "其他"
End Select
%>
```

**预期结果**: 显示 "星期三"

---

### 3.3 For 循环
**文件**: `test_007_for.asp`

```asp
<%
Dim i
For i = 1 To 5
    Response.Write "第 " & i & " 次循环<br>"
Next
%>
```

**预期结果**: 输出 5 行，显示循环次数

---

### 3.4 While 循环
**文件**: `test_008_while.asp`

```asp
<%
Dim count
count = 1

While count <= 3
    Response.Write "Count = " & count & "<br>"
    count = count + 1
Wend
%>
```

**预期结果**: 输出 3 行

---

## 四、函数与过程测试

### 4.1 Function 函数
**文件**: `test_009_function.asp`

```asp
<%
Function Add(a, b)
    Add = a + b
End Function

Dim result
result = Add(10, 20)
Response.Write "10 + 20 = " & result
%>
```

**预期结果**: 显示 `10 + 20 = 30`

---

### 4.2 Sub 过程
**文件**: `test_010_sub.asp`

```asp
<%
Sub Greet(name)
    Response.Write "你好, " & name & "!"
End Sub

Call Greet("李四")
%>
```

**预期结果**: 显示 `你好, 李四!`

---

## 五、字符串操作测试

### 5.1 字符串连接
**文件**: `test_011_string_concat.asp`

```asp
<%
Dim firstName, lastName, fullName
firstName = "王"
lastName = "小明"
fullName = firstName & lastName
Response.Write "全名: " & fullName
%>
```

**预期结果**: 显示 `全名: 王小明`

---

### 5.2 字符串函数
**文件**: `test_012_string_functions.asp`

```asp
<%
Dim text
text = "Hello World"

Response.Write "长度: " & Len(text) & "<br>"
Response.Write "左: " & Left(text, 5) & "<br>"
Response.Write "右: " & Right(text, 5) & "<br>"
Response.Write "大写: " & UCase(text) & "<br>"
Response.Write "小写: " & LCase(text)
%>
```

**预期结果**: 正确显示各字符串函数的结果

---

## 六、数学运算测试

### 6.1 基本运算
**文件**: `test_013_math_basic.asp`

```asp
<%
Dim a, b
a = 15
b = 4

Response.Write "加: " & (a + b) & "<br>"
Response.Write "减: " & (a - b) & "<br>"
Response.Write "乘: " & (a * b) & "<br>"
Response.Write "除: " & (a / b) & "<br>"
Response.Write "整除: " & (a \ b) & "<br>"
Response.Write "取模: " & (a Mod b)
%>
```

**预期结果**:
- 加: 19
- 减: 11
- 乘: 60
- 除: 3.75
- 整除: 3
- 取模: 3

---

### 6.2 数学函数
**文件**: `test_014_math_functions.asp`

```asp
<%
Response.Write "Abs(-5) = " & Abs(-5) & "<br>"
Response.Write "Round(3.14159, 2) = " & Round(3.14159, 2) & "<br>"
Response.Write "Int(5.7) = " & Int(5.7) & "<br>"
Response.Write "Fix(5.7) = " & Fix(5.7) & "<br>"
Response.Write "Sqr(16) = " & Sqr(16)
%>
```

**预期结果**: 正确显示各数学函数的结果

---

## 七、数组测试

### 7.1 静态数组
**文件**: `test_015_array_static.asp`

```asp
<%
Dim fruits(2)
fruits(0) = "苹果"
fruits(1) = "香蕉"
fruits(2) = "橙子"

Dim i
For i = 0 To 2
    Response.Write fruits(i) & "<br>"
Next
%>
```

**预期结果**: 列出三种水果

---

### 7.2 动态数组
**文件**: `test_016_array_dynamic.asp`

```asp
<%
Dim numbers()
ReDim numbers(2)
numbers(0) = 10
numbers(1) = 20
numbers(2) = 30

ReDim Preserve numbers(4)
numbers(3) = 40
numbers(4) = 50

For Each num In numbers
    Response.Write num & "<br>"
Next
%>
```

**预期结果**: 输出 1 到 5 的数字

---

## 八、Include 指令测试

### 8.1 File Include
**文件**: `test_017_include_file.asp`

```asp
<!--#include file="header.asp"-->
<div>
    <p>这是页面内容</p>
</div>
<!--#include file="footer.asp"-->
```

**预期结果**: 正确引入 header 和 footer 文件

---

### 8.2 Virtual Include
**文件**: `test_018_include_virtual.asp`

```asp
<!--#include virtual="/includes/common.asp"-->
<p>内容区域</p>
```

**预期结果**: 从网站根目录引入文件

---

## 九、Request 对象测试

### 9.1 QueryString
**文件**: `test_019_querystring.asp`

```asp
<%
Dim name
name = Request.QueryString("name")
If name = "" Then name = "访客"
Response.Write "欢迎, " & name
%>
```

**测试 URL**: `test_019_querystring.asp?name=张三`

**预期结果**: 显示 `欢迎, 张三`

---

### 9.2 Form 提交
**文件**: `test_020_form.asp`

```asp
<%
If Request.ServerVariables("REQUEST_METHOD") = "POST" Then
    Dim username
    username = Request.Form("username")
    Response.Write "提交的用户名: " & username
Else
%>
<form method="post" action="">
    <input type="text" name="username">
    <input type="submit" value="提交">
</form>
<% End If %>
```

**预期结果**: POST 后显示提交的用户名

---

## 十、错误处理测试

### 10.1 On Error Resume Next
**文件**: `test_021_error_handler.asp`

```asp
<%
On Error Resume Next
Response.Write "开始执行<br>"
Result = 1 / 0  ' 故意制造错误
If Err.Number <> 0 Then
    Response.Write "捕获错误: " & Err.Description
    Err.Clear
End If
Response.Write "<br>继续执行"
%>
```

**预期结果**: 捕获除零错误并继续执行

---

## 测试执行记录

| 编号 | 测试项 | 状态 | 备注 |
|------|--------|------|------|
| 1.1 | 简单文本输出 | ✅ | 通过 |
| 1.2 | HTML 混合输出 | ⚠️ | HTML正确，Now函数未实现 |
| 2.1 | Dim 声明 | ✅ | 需使用单独Dim语句 |
| 2.2 | 未定义变量 | ✅ | 通过 |
| 3.1 | If/Then/Else | ✅ | 通过 |
| 3.2 | Select Case | ✅ | 通过 |
| 3.3 | For 循环 | ✅ | 通过 |
| 3.4 | While 循环 | ✅ | 通过 |
| 4.1 | Function 函数 | ⚠️ | 返回值为空，解析OK |
| 4.2 | Sub 过程 | ⚠️ | 参数传递有问题，解析OK |
| 5.1 | 字符串连接 | ✅ | 通过 |
| 5.2 | 字符串函数 | ✅ | Len/Left/Right/UCase/LCase已实现 |
| 6.1 | 基本运算 | ✅ | 通过 |
| 6.2 | 数学函数 | ✅ | Abs/Round/Int/Fix/Sqr已实现 |
| 7.1 | 静态数组 | ❌ | 不支持索引赋值 |
| 7.2 | 动态数组 | ❌ | 语法错误 |
| 8.1 | File Include | ✅ | 通过 |
| 8.2 | Virtual Include | ✅ | 通过（使用file替代） |
| 9.1 | QueryString | ✅ | 通过 |
| 9.2 | Form 提交 | ❌ | End If跨代码块语法错误 |
| 10.1 | 错误处理 | ❌ | On Error未实现 |
| - | 无括号调用 | ✅ | func arg1, arg2 语法支持 |

---

## 图例说明

- ✅ 通过
- ❌ 失败
- ⚠️ 部分通过
- ⬜ 未测试
